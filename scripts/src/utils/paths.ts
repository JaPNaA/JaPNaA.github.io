const contentRoot = "./docs/assets/content";

const paths = {
    contentRoot: contentRoot,
    v2Content: contentRoot + "/src/v2",
    v2Out: contentRoot + "/v2",
    v1Out: contentRoot + "/v1",
    index: contentRoot + "/index.json"
};

export default paths;